{
  "name": "rollup-copy-smart",
  "version": "1.0.0",
  "description": "a rollup copy plugin",
  "main": "lib/index.js",
  "scripts": {
    "dev": "rm -rf lib/ && tsc -w",
    "build": "rm -rf lib/ && tsc",
    "test": "vitest --run"
  },
  "keywords": [
    "rollup",
    "copy",
    "plugin"
  ],
  "author": "TrickyPi",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "memfs": "^3.4.1",
    "rollup": "^2.67.2",
    "typescript": "^4.5.5",
    "vitest": "^0.3.3"
  },
  "dependencies": {
    "fs-extra": "^10.0.0",
    "chokidar": "^3.5.3"
  }
}
